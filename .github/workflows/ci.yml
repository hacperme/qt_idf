name: CI
on: 
  push:

jobs:
  build_job:
    name: build job
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: run cmd
      run: env.bat && config.bat && make -j32
      shell: cmd